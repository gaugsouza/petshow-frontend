import { UsuarioServiceMock } from './usuario-service-mock';

describe('UsuarioServiceMock', () => {
  it('should create an instance', () => {
    expect(new UsuarioServiceMock()).toBeTruthy();
  });
});
